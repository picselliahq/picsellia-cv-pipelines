[project]
name = "picsellia-cv-pipelines"
version = "0.1.0"
description = ""
authors = [
    {name = "SoniaGrh", email = "sonia.garrouch@hotmail.fr"}
]
dependencies = [
    "picsellia-pipelines-cli",
    "picsellia-cv-engine",
]
requires-python = ">=3.10,<3.14"
readme = "README.md"


[tool.ruff.lint]
select = [
    "E",  # pycodestyle errors
    "W",  # pycodestyle warnings
    "F",  # pyflakes
    "I",  # isort
    "C",  # flake8-comprehensions
    "B",  # flake8-bugbear
    "UP", # pyupgrade
]
ignore = [
    "E501",  # line too long, handled by black
]

[tool.uv.sources]
picsellia-cv-engine = { git = "https://github.com/picselliahq/picsellia-cv-engine", branch = "feat/37-add-assets_ids-in-context" }
