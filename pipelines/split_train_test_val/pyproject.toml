[project]
name = "split_train_test_val"
version = "0.1.0"
description = "Picsellia processing pipeline"
requires-python = ">=3.10,<3.12"

dependencies = [
    "picsellia-pipelines-cli",
    "picsellia-cv-engine>=0.5.2",
    "ultralytics",
    "opencv-python"
]
