[project]
name = "training"
version = "0.1.0"
description = "SAM2 training pipeline"
requires-python = ">=3.10,<3.12"

dependencies = [
    "picsellia-pipelines-cli",
    "picsellia-cv-engine[sam2]",
]
