[project]
name = "pre_annotation"
version = "0.1.0"
description = "Picsellia processing pipeline"
requires-python = ">=3.10,<3.12"

dependencies = [
    "picsellia-cv-engine[ultralytics]>=0.5.0",
    "picsellia-pipelines-cli",
    "onnx"
]
