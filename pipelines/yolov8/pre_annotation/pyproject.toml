[project]
name = "pre_annotation"
version = "0.1.0"
description = "Picsellia processing pipeline"
requires-python = "<3.14"

dependencies = [
    "picsellia-cv-engine",
    "picsellia-pipelines-cli",
    "onnx",
    "torch==2.7.1",
    "torchvision==0.22.1",
    "ultralytics>=8.3.145",
]
