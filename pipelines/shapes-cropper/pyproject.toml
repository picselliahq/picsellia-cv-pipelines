[project]
name = "shapes-cropper"
version = "0.1.0"
description = "Picsellia processing pipeline"
requires-python = ">=3.14"

dependencies = [
    "picsellia-cv-engine",
    "picsellia-pipelines-cli",
]
